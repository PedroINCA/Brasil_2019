#Please write a function called updateZoo. This function should accept three arguments: 1) 
#a vector of strings containing animal names, 2) an animal to remove from the "zoo", and 
#3) an animal to replace it with. The function should remove the requested animal and replace 
#it with the animal that has been requested to be added. (Important: the new animal should end 
#up in the same position in the vector as the animal that was removed. Think of this as a zoo 
#where one animal's cage is cleaned out and a new animal moves in.) Finally, your function 
#should return the modified vector

animal = c("cow", "bird", "lizard")
name1 = "cow"
name2 = "dog"
animal["bird"] = "dog"
animal[which(animal=="bird")] = "dog"

updateZoo = function(x,name1,name2) {return(print(c(x["name1"]=="name2")))}

updateZoo = function(x,name1,name2) {return(print(x[name1]==name2))}

updateZoo = function(x,name1,name2) {return(print(x[which(x=="name1")]=="name2"))}
updateZoo = function(x,name1,name2) {return(print(x[name1]==name2))}

updateZoo = function(x,name1,name2) {which(x==name1)=y} {return(print(x[which(x=="name1")]=="name2"))}

updateZoo(animal,"name1","name2") {y==which(animal=="name1") return(print(x[y]=="name2"))}

animal = c("cow", "bird", "lizard")
name1 = "cow"
name2 = "dog"

position = function(animal,name1) {return(print(which(animal==name1)))}
Y = position(animal,name1)
print(Y)

updateZoo = function(animal,Y,name2) {return(c(animal[Y]==name2))}
updateZoo(animal,Y,name2)




function1 = function(animal, name1) {return(print((animal==name1)))}
function1(animal,name1)
animal = c("cow","bird","dog")
name1 = "cow"

trip1_at = c(trip1[1:15])
trip1 = trip1[1:(L1-2)]
trip2 = trip2[1:(L2-2)]
trip1 = trip1[-c(L1-1,L1)]

function2 = function(x) {
  return(function1(x) + 1)
}
print(function2(8))




updateZoo(animal,name1,name2)
[which(animal=="bird")] = "dog"
which (x==name1)=y

which(animal=="cow")
